import React from 'react'
import './styles.css'
import Card from '../Card'
import { dataScrape } from '../../helper';


const CardContainer = ({ people, vehicles, planets }) => {
  // console.log(people)
  
  // const peopleData = people.map(person => {
  //   let scrapedPerson = dataScrape(person)
  //   console.log(scrapedPerson)
  // })
  
  return (
    <div className='card-container'>
      
    </div>
  )
}

export default CardContainer